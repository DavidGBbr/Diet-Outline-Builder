# Unit 1 Code Generation Plan - Monorepo Foundation

## Purpose

Execute Unit 1 implementation exactly as approved: create the pnpm monorepo, migrate the Python backend into `apps/api/app`, remove old root source/test copies, remove old form UI, create a minimal Next.js shell in `apps/web`, introduce lock-file tooling, and preserve backend regression tests.

## Unit Context

- Unit: Unit 1 - Monorepo Foundation
- Depends On: Approved Unit 1 Functional Design, NFR Requirements, and NFR Design.
- Primary Story: Story 6 - Run The Monorepo Locally With Ollama.
- Supporting Stories: Story 1, Story 2, Story 7.
- Security: SECURITY-09 and SECURITY-10 directly apply; SECURITY-04 partially applies through Next.js headers.
- PBT: No implementation in Unit 1; preserve layout for Unit 2 Hypothesis tests.

## Existing Files To Modify Or Move

- Move `src/diet_outline_builder/graph.py` to `apps/api/app/graph.py`.
- Move reusable package exports from `src/diet_outline_builder/__init__.py` to `apps/api/app/__init__.py`.
- Do not migrate `src/diet_outline_builder/web.py` because the old form UI is removed.
- Do not migrate `src/diet_outline_builder/cli.py` or `__main__.py` because CLI is dropped from target monorepo.
- Move/adapt `tests/test_diet_graph.py` to `apps/api/tests/test_diet_graph.py`.
- Do not migrate `tests/test_web_app.py` because the old form UI/API route is removed.
- Delete root `src/` after migration.
- Delete root `tests/` after migration.

## New Files To Create

- `pnpm-workspace.yaml`
- root `package.json`
- `.env.example`
- `apps/api/pyproject.toml`
- `apps/api/app/__init__.py`
- `apps/api/app/graph.py`
- `apps/api/tests/test_diet_graph.py`
- `apps/web/package.json`
- `apps/web/app/layout.tsx`
- `apps/web/app/page.tsx`
- `apps/web/app/globals.css`
- `apps/web/next.config.ts`
- `apps/web/tsconfig.json`

## Generated Lock Files

- `apps/api/uv.lock` generated by `uv lock` or `uv sync`.
- `pnpm-lock.yaml` generated by `pnpm install`.

## Step-by-Step Generation Plan

### Step 1 - Tooling Preflight

- [x] Check `uv --version`; stop if unavailable.
- [x] Check `pnpm --version`; stop if unavailable.
- [x] Check current git status for awareness.

### Step 2 - Create Monorepo Root Files

- [x] Create `pnpm-workspace.yaml` with `apps/*` packages.
- [x] Replace root Python `pyproject.toml` with root monorepo `package.json` and move Python metadata to `apps/api/pyproject.toml`.
- [x] Add root scripts:
  - `dev:api`
  - `dev:web`
  - `test:api`
  - `typecheck:web`
  - `build:web`
- [x] Create root `.env.example` with Ollama/API/Web defaults.

### Step 3 - Create API Package And Migrate Backend Domain Code

- [x] Create `apps/api/app/` and `apps/api/tests/`.
- [x] Create `apps/api/pyproject.toml` using `uv` metadata.
- [x] Move calculation code from `src/diet_outline_builder/graph.py` to `apps/api/app/graph.py`.
- [x] Create `apps/api/app/__init__.py` exporting `build_diet_outline` and `diet_graph`.
- [x] Update imports from `diet_outline_builder` to `app`.
- [x] Do not create a FastAPI form route or `/api/diet` endpoint in Unit 1.
- [x] Do not migrate CLI files.

### Step 4 - Move Backend Tests

- [x] Move/adapt `tests/test_diet_graph.py` to `apps/api/tests/test_diet_graph.py`.
- [x] Update imports to `from app import build_diet_outline`.
- [x] Do not migrate `tests/test_web_app.py`.

### Step 5 - Remove Legacy Root Python Sources

- [x] Delete root `src/`.
- [x] Delete root `tests/`.
- [x] Verify no duplicate Python app package remains at root.

### Step 6 - Create Minimal Next.js Web Shell

- [x] Create `apps/web/package.json` with Next.js, React, TypeScript, and scripts.
- [x] Create `apps/web/app/layout.tsx`.
- [x] Create `apps/web/app/page.tsx` with minimal placeholder page and no form UI.
- [x] Create `apps/web/app/globals.css`.
- [x] Create `apps/web/tsconfig.json`.
- [x] Create `apps/web/next.config.ts` with initial security headers.

### Step 7 - Generate Lock Files

- [x] Run `uv lock` in `apps/api`.
- [x] Run `pnpm install` at root.
- [x] Verify `apps/api/uv.lock` exists.
- [x] Verify `pnpm-lock.yaml` exists.

### Step 8 - Verification

- [x] Run `pnpm test:api`.
- [x] Run `pnpm typecheck:web`.
- [x] Run `pnpm build:web`.
- [x] Fix Unit 1 issues if any verification command fails.

### Step 9 - AI-DLC Code Summary Artifacts

- [x] Create `aidlc-docs/construction/unit-1-monorepo-foundation/code/code-summary.md` listing created, moved, modified, and deleted files.
- [x] Update this code-generation plan checkboxes.
- [x] Update `aidlc-docs/aidlc-state.md` and `aidlc-docs/audit.md`.

## Story Traceability

- Story 6: Primary. Unit 1 creates the monorepo, local scripts, env template, and lock-file foundation.
- Story 1: Supporting. Unit 1 creates the frontend app path/shell for later chat UI.
- Story 2: Supporting. Unit 1 creates backend package layout for later extraction.
- Story 7: Supporting. Unit 1 implements lock-file and minimal-scaffold security responsibilities.

## Security Compliance Plan

- SECURITY-09: Remove old form UI and avoid adding public diet/form routes.
- SECURITY-10: Generate `uv.lock` and `pnpm-lock.yaml`; use official registries via uv/pnpm defaults.
- SECURITY-04: Add initial Next.js security headers in `next.config.ts`.
- SECURITY-05: Defer WebSocket/API schema validation to later units; no public API created in Unit 1.

## PBT Compliance Plan

- PBT is N/A for Unit 1.
- Backend test structure will support Unit 2 Hypothesis tests.

## Approval Gate

This plan is the single source of truth for Unit 1 Code Generation. Code generation must not start until the user explicitly approves this plan.
